function dateDifferenceInDays(day1, month1, year1, day2, month2, year2) {
    return getInUnixDays(day2, month2, year2) - getInUnixDays(day1, month1, year1);
}
function isLeap(year) {
    if (year % 400 == 0 || year % 4 == 0 && year %100 != 0);
    return 
}
function getInUnixDays(day, month, year) {
    // const a=1;
    const startUnixYear = 1970;
    const 
}